import { Widget } from "@/app/projects/test/_components/layout/properties/inputs/config";

export function SelectInput({}: Widget["SELECT"]) {
  return (
    <select className="block">
      <option></option>
    </select>
  );
}
